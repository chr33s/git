diff --git a/node_modules/effect/dist/Schema.d.ts b/node_modules/effect/dist/Schema.d.ts
index 9547bd0..1198ec5 100644
--- a/node_modules/effect/dist/Schema.d.ts
+++ b/node_modules/effect/dist/Schema.d.ts
@@ -9856,6 +9856,8 @@ export declare const TaggedError: {
         } & S["fields"]>>, Cause_.YieldableError & Brand>;
     };
 };
+/** Alias of `TaggedError`, under the name later betas use (see patches/). */
+export declare const TaggedErrorClass: typeof TaggedError;
 /**
  * Represents a function that builds a fast-check `Arbitrary<T>` from the
  * `fast-check` module.
diff --git a/node_modules/effect/dist/Schema.js b/node_modules/effect/dist/Schema.js
index 44a28b6..bd59beb 100644
--- a/node_modules/effect/dist/Schema.js
+++ b/node_modules/effect/dist/Schema.js
@@ -9204,6 +9204,16 @@ export const Error = identifier => (schema, annotations) => {
  * @category constructors
  * @since 3.10.0
  */
+/**
+ * Alias of {@link TaggedError}, under the name later betas use.
+ *
+ * Added by `patches/effect+4.0.0-beta.107.patch`: `@distilled.cloud/*` (a
+ * transitive dependency of `alchemy`) is built against the newer spelling,
+ * and the two are the same constructor. Delete the patch when this repo
+ * moves to a beta that ships the name itself.
+ */
+export const TaggedErrorClass = identifier => TaggedError(identifier);
+
 export const TaggedError = identifier => {
   return (tagValue, schema, annotations) => {
     const struct = isStruct(schema) ? schema.mapFields(fields => ({
diff --git a/node_modules/effect/dist/unstable/cli/Command.d.ts b/node_modules/effect/dist/unstable/cli/Command.d.ts
index caf4fd9..07a911f 100644
--- a/node_modules/effect/dist/unstable/cli/Command.d.ts
+++ b/node_modules/effect/dist/unstable/cli/Command.d.ts
@@ -2356,3 +2356,5 @@ export declare const runWith: <const Name extends string, Input, E, R, ContextIn
 }) => (input: ReadonlyArray<string>) => Effect.Effect<void, Exclude<E, Terminal.QuitError> | CliError.CliError, R | Environment>;
 export {};
 //# sourceMappingURL=Command.d.ts.map
+/** Hides a command from help output. Added by patches/ for alchemy's CLI. */
+export declare const withHidden: <Name extends string, Input, E, R>(self: Command<Name, Input, E, R>) => Command<Name, Input, E, R>;
diff --git a/node_modules/effect/dist/unstable/cli/Command.js b/node_modules/effect/dist/unstable/cli/Command.js
index 248bbba..37a5727 100644
--- a/node_modules/effect/dist/unstable/cli/Command.js
+++ b/node_modules/effect/dist/unstable/cli/Command.js
@@ -492,6 +492,18 @@ export const withDescription = /*#__PURE__*/dual(2, (self, description) => makeC
   ...toImpl(self),
   description
 }));
+/**
+ * Hides a command from help output and completions.
+ *
+ * Added by `patches/effect+4.0.0-beta.107.patch`: `alchemy`'s CLI is built
+ * against a later beta that ships this. The flag is recorded on the command
+ * and ignored by this version's help renderer, so the command still works —
+ * it is simply not hidden. Delete the patch when the pinned version has it.
+ */
+export const withHidden = self => makeCommand({
+  ...toImpl(self),
+  hidden: true
+});
 /**
  * Sets a short description for a command.
  *
